# MESLite Cursor Rules

## 0) 适用范围
- 本规则适用于本项目所有代码、配置、文档与自动化流程。
- 任何实现都必须先满足本规则，再考虑扩展功能。

## 1) 技术栈与资源约束（永久免费优先）
- 仅使用 Vercel 免费环境可支持的部署方式与能力。
- 仅引入免费可用的依赖、服务与资源（含开发期与运行期）。
- 禁止引入任何需要付费订阅才能正常开发、构建、部署或运行的技术栈、平台能力、插件、API 或托管资源。
- 若某方案存在免费/付费两种路径，默认采用免费路径；若不确定成本，先按“可能付费”处理并拒绝采用。

## 2) 产品目标：微型报工系统
- 系统定位为“微型报工系统（Mini MES Report System）”。
- 功能优先级：可用性 > 简洁性 > 可扩展性。
- 在满足核心流程可用前，不做与目标无关的复杂设计。

## 3) 账号与工厂信息流程
- 用户通过邮箱注册账号。
- 注册成功后，允许并引导用户定义自己工厂的基础信息（如工厂名称、联系人、地址等可扩展字段）。
- 每个主账号登录后，可新增约 10 名员工账号（建议上限 10，可配置为 10~12 的小范围）。
- 员工账号用于提交订单报工信息。
- 报工数据必须关联所属工厂与提交员工，确保基础可追溯性。

## 4) 代码完成后的自动发布与部署
- 每次代码完成后，默认流程应包含：
  1. 自动提交并推送到 GitHub 仓库；
  2. 触发并完成 Vercel 自动部署。
- 优先采用 GitHub Actions + Vercel 官方免费集成能力实现 CI/CD。
- 若检测到自动发布/部署失败，必须输出明确失败原因与修复建议。

## 5) 首页登录语言一键切换（中英文）
- 首页登录框必须提供“中文 / English”一键切换按钮。
- 用户在登录前选择中文：进入系统后默认中文界面。
- 用户在登录前选择英文：进入系统后默认英文界面。
- 语言选择应在登录态中保持（至少会话级持久化，推荐用户级持久化）。
- 新增页面与文案必须同步提供中英文资源，不允许仅单语上线。

## 6) 工程执行原则（必须遵守）
- 先思考再编码：先明确目标、边界与验收条件，再动手实现。
- 简单优先：优先最小可行方案，避免过度设计。
- 精准修改：仅改动与当前目标直接相关的代码，控制影响面。
- 目标驱动：所有实现都要服务当前业务目标与可验收结果。

## 7) 实施与验收补充
- 任何新功能提交时，需附最小验证步骤（本地或预览环境）。
- 对关键流程（注册、登录、员工新增、报工提交、语言切换）至少覆盖基础可用测试。
- 文档与配置需保证新成员可在免费资源条件下复现开发与部署流程。
